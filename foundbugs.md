# foundbugs.md — usbliter8-arctic audit

Generated by `--new` audit. Fix order: CRITICAL → HIGH → MEDIUM → LOW.

## Round 1 (core tooling) — all fixed, kept for history


## CRITICAL

- [x] **#1 RestoreRamdisk patches silently discarded** — `cfw_builder.py:patch_restoreramdisk`. Patches written to `RestoreRamdisk.raw` in temp work dir, never re-wrapped or copied back into `ipsw_dir`. FIXED: rewrap with `img4tool -c -t rdsk` back into `Firmware/all_flash/`; failed extraction now hard-fails instead of corrupting the im4p.
- [x] **#2 iBSS re-wrapped with wrong im4p type** — `cfw_builder.py:patch_ibss:122`. `_wrap_raw_to_im4p` called without tag → img4tool default `rkrn`. Must be `ibss`. FIXED: pass `"ibss"`.

## HIGH

- [x] **#3 Hardcoded d421 board for all devices** — `cfw_builder.py`. FIXED: iBSS/iBEC/DeviceTree paths now derive from profile `board:` via `_board_short()`/`_board_config()`, with glob fallback for any `iBSS.*.RELEASE.im4p` etc.
- [x] **#4 Daemon patches never apply** — `cfw_builder.py:patch_userland`. FIXED: missing daemon binaries now fail loudly with remediation text instead of silently skipping; build returns False so CFW is never falsely reported complete.
- [x] **#5 `restore_cfw.sh` existence never checked** — `boot_chain.py:restore_device`. FIXED: make_cfw.py, restore_cfw.sh, tss_proxy_server.py all pre-checked; TSS proxy guarded by DRY_RUN and killed in `finally`.

## MEDIUM

- [x] **#6 Untrusted YAML crashes validator** — `device_offsets.py`. FIXED: type-checked offsets, `_fmt_offset()` safe formatting, `yaml.YAMLError` caught, non-dict `patches` guarded, `list_offset_files`/`find_online_sources` no longer re-crash.
- [x] **#7 TSS proxy Popen on missing file** — `boot_chain.py`. FIXED: existence check + DRY_RUN guard + `finally` cleanup (same edit as #5).
- [x] **#8 `setup_usb_network`/`setup_vnc` CWD-relative path** — `boot_chain.py`. FIXED: new `_find_script()` resolves scripts in CWD or known work dirs (verified live: finds net_up.sh), runs with cwd=script dir.
- [x] **#9 Health check checks PATH not `tools/`** — `hardware_guide.py`. FIXED: checks `TOOLS_DIR` first with PATH fallback; bundled usbliter8ctl now reported found.

## LOW

- [x] **#10 Temp dirs leak** — `cfw_builder.py`. FIXED: intermediate `work_dir` cleaned in `finally`; patched `ipsw_dir` (the product) intentionally kept.
- [x] **#11 Hardcoded `/home/kaffein/...` paths** — `main.py`, `cfw_builder.py`, `boot_chain.py`. FIXED: replaced with `Path.home()`.
- [x] **#12 Password in argv** — `boot_chain.py`. FIXED: sshpass now uses `-e` + `SSHPASS` env; `_run()` gained `env=` kwarg.
- [x] **#13 UF2 magic accepts `0x00000000`** — `hardware_guide.py`. FIXED: removed; 3 real magics remain.
- [x] **#14 `serial[:40]`/`[:60]` crash on None** — `pwn_utils.py`. FIXED: `(x or "")[:60]` / `(x or "?")[:40]`.
- [x] **#15 Dead code** — `boot_chain.py`. FIXED: unused `sudo_askpass`/`env` block removed in #5 edit.

---

## Round 2 — offset migration feature (audit of fingerprint.py / migrate.py / tests)

### HIGH

- [x] **#16 Bootstrap profile silently incomplete** — `migrate.py:apply_offsets`. FIXED: upsert — missing sections (txm) created from the base profile's container type, missing entries (kernel 15/18) appended from base entry shape. Verified: bootstrap now writes txm 6/6 + kernel 18/18; regression test added.

### MEDIUM

- [x] **#17 `mask_insn` mis-masks `subs` immediate** — `fingerprint.py`. FIXED: add/sub check moved before ldr/str-imm; unit test asserts `subs x2,x2,#0x850` → `0xF1000142` (shift bits zeroed).

### LOW

- [x] **#18 Dead code** — `migrate.py`. FIXED: removed `import shutil`; `migrate_section()` signature reduced to `(base_profile, section, comps)`; callers updated.
- [x] **#19 `cli_main` uncaught `_load_yaml` ValueError** — `migrate.py`. FIXED: try/except → clean error message.
- [x] **#20 Template sentinel values pass validation** — `device_offsets.py`. FIXED: `"?"` placeholders now rejected by `_is_valid_patch_value`; `_update_entry` fills `"????????"` values from the target site word (`value_filled_from_site` metadata). Tested both paths.
- [x] **#21 Cosmetic: `disasm_class_ok` reflects first hit** — `fingerprint.py`. FIXED: reports the verification of the actually chosen target offset.
